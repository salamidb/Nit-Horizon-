@model IEnumerable<Knith_Horizon.Models.AttendIntegrity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CheckFDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CheckTDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Checked)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CheckFDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CheckTDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Checked)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Eno }) |
            @Html.ActionLink("Details", "Details", new { id=item.Eno }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Eno })
        </td>
    </tr>
}

</table>
